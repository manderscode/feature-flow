# v0 runtime files (should only exist in preview, not in git)
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts

# Common ignores
node_modules/
.next/
.env*.local
.DS_Store

# Convex
.convex/
# Note: convex/_generated/ should NOT be gitignored for Vercel builds
# The files are generated during build, but we need to ensure they're accessible
# Uncomment below only if you want to exclude them (not recommended for Vercel):
# convex/_generated/